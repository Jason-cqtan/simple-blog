# simple-blog environment configuration
# Copy this file to .env and adjust values for your environment.
# Load with: export $(grep -v '^#' .env | xargs)

# ── Database ─────────────────────────────────────────────────────────────────
# Supported drivers: postgres, mysql
DB_DRIVER=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=blog_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_NAME=simple_blog

# ── Server ───────────────────────────────────────────────────────────────────
SERVER_PORT=8080

# ── Security ─────────────────────────────────────────────────────────────────
# IMPORTANT: Replace with a long random string in production.
JWT_SECRET=change-me-to-a-long-random-secret

# Set to true when serving over HTTPS (production).
SECURE_COOKIE=false
